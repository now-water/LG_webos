import base64
import asyncio
import websockets

host
port

server_webOS = websockets.serve(accept_webOS, host, port)
#getImageFromDetection()
asyncio.get_event_loop().run_until_complete(server_webOS)
asyncio.get_event_loop().run_forever()